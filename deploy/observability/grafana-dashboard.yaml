apiVersion: v1
kind: ConfigMap
metadata:
  name: meridian-grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"          # Grafana sidecar watches this label
data:
  meridian-dashboard.json: |-
